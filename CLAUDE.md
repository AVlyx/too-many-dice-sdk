# too-many-dice SDK

## Running commands (node / npm / npx)

Node is managed via nvm and is not on the default PATH. Load nvm before any node/npm/npx command:

```bash
source ~/.zshrc
```

Example — run tests:

```bash
source ~/.zshrc && npm test
```
